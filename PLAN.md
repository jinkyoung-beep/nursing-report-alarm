# PLAN — 간호인력 신고 알리미 (PRD 기반 구현 계획)

## 이번 사이클 목표
PRD.md에 정의된 must 기능 2개(신고 항목 등록·관리, 알림 발송)를 모두 구현해서, 정기형·수시형 신고 마감을 이메일로 놓치지 않고 안내받을 수 있는 앱을 완성한다.

## 성공 기준
- [ ] 정기형 8개 항목을 엑셀로 일괄 업로드하면 지정된 열(신고 항목명·시작일·반복 주기)만 정확히 등록된다.
- [ ] 수시형(간호간병통합서비스 수시신고) 발생일을 입력하면 마감일(발생일+6일)이 자동 계산된다.
- [ ] 정기형은 항목별 규칙대로(기본 D-7/D-3/D-day, 예외 D-10 포함 4회, 특수 항목은 매월 16일 1회) 이메일 알림이 온다.
- [ ] 수시형은 발생일+1일, 마감일-1일에 이메일 알림이 온다.
- [ ] 비밀번호 로그인 없이는 화면에 접근할 수 없다.
- [ ] 신고 처리를 마감 이후로 늦게 해도, 다음 시작일은 원래 예정된 시작일 기준으로 계산된다.
- [ ] 완료 처리된 항목은 이후 알림이 더 이상 오지 않는다.
- [ ] 엑셀에 주민등록번호·이름 등이 포함돼 있어도 DB에는 저장되지 않는다.
- [ ] 차등제 항목을 보면 지난 차등제 완료 이후 등록된 수시형 항목 목록이 함께 보인다.
- [ ] `npm run lint`, `npm run build`가 오류 없이 통과한다.

## 작업 순서 (먼저 되면 나머지가 쉬워지는 것부터)
1. 신고 항목을 저장할 데이터 구조(DB 스키마: 신고 항목명·시작일·반복 주기·유형(정기/수시)·발생일·상태) 만들기
2. 비밀번호 로그인 기능 만들기 (로그인 전에는 나머지 화면 접근 차단)
3. 정기형 신고 엑셀 업로드 기능 만들기 (지정된 열만 읽어서 저장 — 열 이름 자체가 없으면 업로드 차단, 값이 빈 행이 있으면 몇 번째 줄인지 안내)
4. 등록된 신고 항목 목록 보기 화면 만들기 (정기형/수시형 함께, 여러 건 동시 표시. 차등제 항목은 지난 완료 시점 이후 등록된 수시형 목록도 함께 보여주기)
5. 신고 항목 개별 추가·수정 화면 만들기 (반복 주기는 정해진 목록 중 선택)
6. 수시형 신고 발생일 입력 및 마감일(발생일+6일) 자동 계산 기능 만들기
7. 신고 처리 완료 표시 기능 만들기
8. 처리 완료 후 다음 시작일 자동 계산 기능 만들기 (반복 주기 기준, 지연 처리 시에도 원래 예정 시작일 기준 유지)
9. 알림 발송 조건(기본 3회/예외 4회/특수 매월 16일 1회/수시형 2회) 판단 로직 만들기 — PRD.md 5번 표의 항목명과 자동 대조
10. 이메일 발송 연동 기능 만들기
11. 매일 한국시간(KST) 오전 9시에 알림 조건을 확인해 이메일을 보내는 자동 실행 기능 만들기 (`CRON_SECRET`으로 요청 검증)
12. 처리 완료 표시된 항목은 이후 알림이 가지 않는지 확인하는 예외 처리 넣기
13. Vercel 배포 및 환경 변수 설정하기 (`APP_PASSWORD`, `GMAIL_USER`/`GMAIL_APP_PASSWORD`, `NOTIFY_TO_EMAIL`, `SUPABASE_URL`/`SUPABASE_ANON_KEY`, `CRON_SECRET` 등 확인)
