# DESIGN — 간호인력 신고 알리미

## 1. 화면 구성

| 화면 | 무엇이 보이는지 |
|---|---|
| ① 로그인 화면 | 비밀번호 입력 칸 1개 + 로그인 버튼. 이 화면을 통과해야만 다른 화면에 들어갈 수 있다. |
| ② 신고 항목 목록 화면 (로그인 후 첫 화면) | 등록된 정기형·수시형 신고를 카테고리(간호인력 차등제 / 간호간병통합서비스 / 제2차 교대제 시범사업 / 기타)별로 묶어서 보여준다. 카테고리 제목마다 다른 색을 써서 한눈에 구분되게 하고, 같은 카테고리 안에서는 이름이 같은 것끼리 먼저 모으고(예: 야간전담간호사 변경신고 병동별 3건) 그 안에서 날짜가 가까운 순으로 정렬한다("기타"는 항상 맨 아래). 각 줄에는 신고 항목명, 다음 알림/마감까지 남은 일수, 처리 상태(대기/완료)가 보이고, "완료 처리"·"수정"(④ 화면으로 이동)·"삭제" 버튼이 있다. 화면 상단에는 "엑셀 업로드"·"수시신고 등록"(수시형 발생일 입력 상태로 바로 시작하는 ④ 화면 바로가기)·"새 항목 추가" 버튼이 있다. 간호인력 차등제 신고 줄을 펼치면, 지난 차등제 완료 이후 등록된 수시형(수시신고) 항목 목록(예: "A간호사 병가 7/20~8/28")이 함께 보인다. |
| ③ 엑셀 업로드 화면(또는 팝업) | 파일 선택 버튼과 업로드 버튼만 있는 단순한 화면. 지정된 열 이름(신고 항목명·시작일·반복 주기) 자체가 없으면 업로드를 막고 어떤 열이 빠졌는지 알려주고, 열은 있지만 특정 행의 값이 비어 있으면 몇 번째 줄의 어떤 값이 비었는지 알려준다. |
| ④ 신고 항목 추가·수정 화면(또는 팝업) | 신고 항목명 입력 칸, 유형 선택(정기형/수시형), 시작일 또는 발생일 입력 칸, 반복 주기 선택 칸(정기형일 때만, 드롭다운: 30일/매월/2개월/분기/6개월/연 1회/비정기)으로 구성된다. 알림 유형(기본/예외/특수/준비)은 사용자가 고르지 않고, 신고 항목명을 PRD.md 5번 표와 자동 대조해서 시스템이 정한다. 수시형을 선택하면 화면 제목이 "간호간병통합서비스 수시신고"로 바뀐다. 수시형이거나, 정기형 중 같은 이름으로 여러 건이 동시에 존재할 수 있는 항목(간호간병통합서비스 야간전담간호사 변경신고)이면, 자유롭게 적는 메모 칸(선택 입력, 예: "본관20층"처럼 구분용으로 적는 용도)이 함께 보인다. |

## 2. 데이터 흐름 (입력 → 처리 → 출력)

1. **입력**: 엑셀 업로드(최초 1회, 정기형 12개 항목) 또는 화면에서 개별 추가/수정(정기형 신규 항목, 수시형 발생일 입력)
2. **처리 – 저장**: 서버가 지정된 열/값만 읽어서 검증 후 데이터 저장소에 저장한다 (주민등록번호 등 다른 정보는 저장하지 않음)
3. **처리 – 판단**: 매일 한국시간(KST) 오전 9시에 서버가 오늘 날짜와 저장된 시작일·발생일을 비교해, 알림을 보내야 할 항목(기본 D-7/D-3/D-day, 예외 D-10/D-7/D-3/D-day, 특수 항목은 매월 16일, 준비 항목은 시작일 1회, 수시형 발생일+1·마감-1)을 찾아낸다. 이 요청은 `.env`의 `CRON_SECRET` 값이 일치할 때만 처리한다.
4. **출력 – 알림**: 해당 항목이 있으면 이메일 발송 서비스를 통해 `.env`의 `NOTIFY_TO_EMAIL` 주소로 이메일을 보낸다
5. **처리 – 완료 반영**: 사용자가 목록 화면에서 "완료 처리"를 누르면, 서버가 완료 상태로 저장하고 정기형은 반복 주기만큼 다음 시작일을 자동으로 계산해 갱신한다 (계산 기준은 실제 완료 날짜가 아니라 원래 예정된 시작일이며, 더 이상 그 회차의 알림은 가지 않음)
6. **참고 – 연관 정보 제공**: 간호인력 차등제 항목을 볼 때는, 서버가 "지난 차등제 완료 시점 ~ 이번 차등제 시작일" 구간의 occurred_date를 가진 수시형 항목들을 조회해서 함께 보여준다 (신고 누락 방지용 참고 정보이며, 별도 알림을 새로 만들지는 않음)

## 3. 기술 선택

| 구성 요소 | 선택 | 한 줄 설명 |
|---|---|---|
| 화면 + 서버(API) | **Next.js** (고정) | 화면과 서버 쪽 코드를 한 프로젝트에서 함께 만들 수 있는 도구. Part 5에서 Vercel로 그대로 배포한다. |
| 배포 | **Vercel** (고정) | 만든 앱을 인터넷에 올려서 실제 주소로 접속할 수 있게 해주는 서비스. |
| 데이터 저장소 | **Supabase** (확정) | 신고 항목·날짜 같은 정보를 저장해두는 "온라인 창고" 서비스. `.env`의 `SUPABASE_ACCESS_TOKEN`은 Supabase CLI 관리용이고, 앱이 실제로 데이터를 읽고 쓰려면 프로젝트 URL·API 키가 별도로 더 필요하다 (`SUPABASE_URL`, `SUPABASE_ANON_KEY`). |
| 로그인 방식 | **비밀번호 1개 대조 방식** (확정) | 회원가입 시스템을 따로 만들지 않고, `.env`의 `APP_PASSWORD`와 입력값이 같은지만 확인하는 가장 단순한 방식. "혼자 쓰는 앱"이라는 결정과 맞는다. |
| 엑셀 읽기 | **xlsx** (확정) | 업로드된 엑셀 파일을 열어서 안의 내용을 읽어주는 코드 부품(라이브러리). |
| 자동 실행 예약 | **Vercel Cron Jobs** (확정) | 이미 쓰기로 한 Vercel이 제공하는 기능으로, "매일 몇 시에 이 작업을 실행해줘"라고 예약해두면 자동으로 실행해준다. 별도 서비스 가입이 필요 없다. 요청은 `.env`의 `CRON_SECRET` 값으로 검증해서, 외부에서 아무나 호출하지 못하게 막는다. |
| 이메일 발송 | **Gmail(내 메일 계정)로 직접 발송** | 새로 가입할 필요 없이 지금 쓰는 Gmail 계정으로 보내는 방식. "앱 비밀번호"라는 별도 설정을 한 번 해두면, 그 값으로 서버가 이메일을 대신 보낸다. |

> `.env`에 저장해서 쓰는 값: `APP_PASSWORD`(로그인 비밀번호), `GMAIL_USER`/`GMAIL_APP_PASSWORD`(이메일 발송 계정), `NOTIFY_TO_EMAIL`(알림 받을 주소), `SUPABASE_URL`/`SUPABASE_ANON_KEY`(앱 실행용 DB 접속 정보), `CRON_SECRET`(자동 실행 요청 검증) — CLAUDE.md의 비밀값 관리 규칙을 그대로 따른다.
>
> **한국시간(KST) 기준**: 알림 판단·발송은 모두 한국시간(UTC+9) 기준이며, 매일 오전 9시(KST)에 실행한다. Vercel Cron은 기본이 UTC라 이 부분을 코드에서 명시적으로 변환해야 한다.
>
> **추후 결정할 것** (지금은 미정으로 남겨둠): 엑셀을 중복 업로드했을 때 처리 방식, 이메일 발송이 실패했을 때 재시도 여부, 같은 날 알림 대상이 여러 건이면 메일을 한 통에 모아 보낼지 여러 통으로 보낼지.

## 4. 디자인 톤앤매너

(2026-08-28 수정: 처음엔 더현대 판교점 사진을 참고해 어두운 다크 테마로 만들었는데, 실제로 써보니 어둡고 답답한 느낌이 든다는 피드백을 받아 밝은 톤으로 바꿨다. 따뜻한 금색 포인트는 그대로 유지하고, 배경만 밝게 바꿨다.)

(2026-08-30 수정: 오류 문구가 팔레트 밖의 기본 빨간색을 쓰고 있어서, 따뜻한 톤과 어울리는 오류색을 추가했다. "포인트(진하게)"는 실제로 hover 시 더 진해지는 용도라 이름을 "포인트(더 강하게)"로 정리했다.)

(2026-09-04 수정: 파워포인트 "기술 디자인" 템플릿(짙은 남색 + 그물망 패턴 + 파란 포인트) 분위기를 참고해 다시 다크 테마로 바꿨다. 다만 원본 이미지보다 배경을 조금 밝게 조정하고, 글자 색은 뚜렷하게 대비되도록 골랐다. 라이트 테마는 쓰지 않고 다크 테마 하나로 다시 고정한다.)

(2026-09-04 추가 수정: 그물망(네트워크 매쉬) 장식도 함께 넣기로 했다. 사진이 아니라 점·선을 직접 그린 SVG이며, 글자와 겹치지 않는 화면 상단 배경에만 낮은 투명도(20%)로 넣어서 본문 글자의 가독성을 해치지 않게 했다.)

(2026-09-04 네 번째 수정: 목록 화면을 카테고리(간호인력 차등제/간호간병통합서비스/제2차 교대제 시범사업/기타)로 묶어서 보여주기로 하면서, 카테고리 제목마다 다른 색을 넣어 한눈에 구분되게 했다.)

| 항목 | 값 | 설명 |
|---|---|---|
| 배경(기본) | `#16213e` | 짙은 남색 (참고 이미지보다 한 톤 밝게 조정) |
| 표면(카드·입력창) | `#1f2c4f` | 배경보다 한 단계 밝은 남색, 카드가 배경 위에 떠 보이도록 구분 |
| 테두리 | `rgba(255,255,255,0.12)` | 옅은 흰색 선, 카드 경계에 사용 |
| 기본 글자 | `#eef2fb` | 거의 흰색에 가까운 밝은 색, 어두운 배경과 뚜렷하게 대비 |
| 보조 글자 | `#a9b8dc` | 설명·부가 정보용 옅은 파란회색 (기본 글자보다는 흐리지만 배경과 충분히 대비됨) |
| 포인트(강조) | `#5b8def` | 버튼·강조 텍스트·다음 알림까지 남은 일수 같은 곳에 사용하는 밝은 파란색 |
| 포인트(더 강하게, hover 등) | `#2f5fd0` | 포인트 색보다 진하고 채도 높은 버전 (hover 시, 일러스트 배지처럼 더 강조할 때) |
| 포인트(연하게, 그림 배경 등) | `#2a3b66` | 일러스트 배경 등 은은한 강조 영역에 사용하는 어두운 남색 |
| 포인트(보조, 그물망 전용) | `#d966a3` | 배경 그물망 장식의 그라데이션 끝 색(분홍빛). 다른 곳에는 쓰지 않는다 |
| 오류(경고 문구) | `#ff6b5b` | 로그인 실패 등 오류 문구에 쓰는, 어두운 배경에서 잘 보이는 밝은 산호색 |
| 카테고리 - 간호인력 차등제 | `#e0a458` | 목록 화면 카테고리 제목 구분용 |
| 카테고리 - 간호간병통합서비스 | `#4fd1a5` | 목록 화면 카테고리 제목 구분용 |
| 카테고리 - 제2차 교대제 시범사업 | `#b48cf2` | 목록 화면 카테고리 제목 구분용 |

- **글꼴**: 지금 쓰고 있는 Geist(고딕 계열)를 그대로 사용한다. 새 글꼴을 추가하지 않는다.
- **일러스트**: 알림종(벨) 모양의 작은 그림을 로그인 화면·홈 화면 상단에 넣는다. 사진이 아니라 팔레트 색으로 직접 그린 단순한 도형 그림이다.
- **배경 장식**: 화면 상단에 점과 선으로 이루어진 그물망 패턴을 낮은 투명도(20%)로 넣는다. 파란색→분홍색 그라데이션을 쓰고, 본문 글자 뒤로는 겹치지 않게 배치해 가독성에 영향을 주지 않는다.
- **분위기**: 여백을 넉넉하게 두고, 화면을 요소로 꽉 채우지 않는다. 제목은 자간을 살짝 넓혀서 차분한 느낌을 낸다.
- **적용 범위**: 로그인 화면부터 목록·업로드·추가 화면까지 전부 이 팔레트를 기본으로 쓴다. 밝은(라이트) 모드는 따로 만들지 않는다 — 이 앱은 사용자 1인 전용이라 다크 테마 하나로 고정한다.
