@AGENTS.md

# 프로젝트 규칙 (간호인력 신고 알리미)

이 프로젝트는 [PRD.md](PRD.md)에 정의된 "간호인력 신고 알리미" 앱이다. 코드를 작성하기 전에 PRD.md를 먼저 확인하고, 여기 규칙과 어긋나는 방향으로 임의로 기능을 추가하거나 바꾸지 않는다.

## 기술 스택
- Next.js로만 개발한다 (화면·API를 한 프로젝트 안에서 처리). 다른 프레임워크로 바꾸거나 마이그레이션하자고 제안하지 않는다.
- 배포는 Vercel을 사용하고, 자동 실행(매일 알림 확인)은 Vercel Cron Jobs를 사용한다.
- 데이터 저장은 Supabase, 엑셀 읽기는 xlsx 라이브러리, 이메일 발송은 Gmail 앱 비밀번호 방식을 사용한다.

## 데이터 처리 규칙
- 신고 항목 엑셀 업로드 시, 지정된 열(신고 항목명·시작일·반복 주기)만 읽어서 저장한다. 그 외 열(주민등록번호·이름 등)은 읽지도 저장하지도 않는다.
- 간호인력 개인의 주민등록번호는 어떤 경우에도 이 앱의 데이터베이스에 저장하지 않는다.
- 반복 주기는 자유 텍스트가 아니라 정해진 목록(30일 / 매월 / 2개월 / 분기 / 6개월 / 연 1회 / 비정기) 중에서만 선택하게 한다.
- 신고 처리가 마감(D-day) 이후로 늦어져도, 다음 시작일은 원래 예정된 시작일 기준으로 계산한다.
- 수시형 신고는 여러 건이 동시에 진행될 수 있으므로, 각 건을 독립된 항목으로 관리한다. 정기형 중 "간호간병통합서비스 야간전담간호사 변경신고"도 병동마다 날짜가 달라 같은 이름으로 여러 건이 동시에 진행될 수 있으며, 마찬가지로 각 건을 독립된 항목으로 관리한다(메모로 병동 구분).

## 알림 규칙
- 정기형 기본 항목(간호간병통합서비스 예정근무 신고, 대체간호사 근무표 입력 = 2개): 시작일 기준 D-7, D-3, D-day 3회
- 정기형 예외 항목(간호인력 차등제 신고, 제2차 간호사 교대제 개선 시범사업 4종, 간호간병통합서비스 야간전담간호사 변경신고 = 총 6개): 시작일 기준 D-10, D-7, D-3, D-day 4회
- 정기형 특수 항목(간호간병통합서비스 정기신고 = 1개): 매월 16일 아침 1회만
- 정기형 준비 항목(간호간병 병동운영환경 자율점검, 교대제 원내자료 취합, 간호인력 야간근무 모니터링 = 총 3개): 시작일 아침 1회만
- 수시형 항목(간호간병통합서비스 수시신고): 발생일+1일 1회, 마감일(발생일+6일)-1일 1회, 총 2회
- 항목별 알림 유형(기본/예외/특수/준비)은 PRD.md 5번 표의 이름과 자동 대조해서 정하며, 화면에서 사용자가 직접 고르지 않는다.
- 알림은 이메일로 발송한다 (받는 주소는 `.env`의 `NOTIFY_TO_EMAIL`).
- 알림 판단·발송은 한국시간(KST, UTC+9) 기준 매일 오전 9시에 실행하고, 자동 실행 요청은 `.env`의 `CRON_SECRET` 값으로 검증한다.
- 알림 예정일이 주말·한국 공휴일과 겹치면 그날 보내지 않고, 그 구간(연휴 포함) 바로 직전 평일 1회 + 바로 직후 평일 1회로 나눠 보낸다 (`date-holidays` 라이브러리로 공휴일 판단).
- 신고 처리 완료로 표시된 항목은 이후 예정된 알림을 더 이상 보내지 않는다.

## 범위 (스코프)
- must 기능은 "신고 항목 등록·관리"와 "알림 발송" 2개뿐이다. 임의로 새 기능을 추가하지 않는다.
- 여러 사용자 계정/공유, 신고 자동 제출·연동, 신고 이력 통계, 엑셀 자동 형식·파일명 인식은 이번 범위에 포함하지 않는다 (자세한 내용은 PRD.md 6번 참고).
- 접근 제한은 비밀번호 로그인 1개로만 한다 (여러 계정을 관리하는 시스템이 아니다).

## 비밀값 관리
- 모든 API 키·비밀값은 `.env`에 두고 코드에 직접 적지 않는다.
- `.env`와 `node_modules` 폴더는 `.gitignore`에 등록해두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 않고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.

## 작업 절차 (검증 루프)
아래 순서를 매번 반복한다.
1. 변경한다
2. `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다
3. 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다
4. 문제가 있으면 고치고 다시 1)로 돌아간다
5. 통과하면 무엇을 왜 바꿨는지 한 줄로 요약한다

## 디자인 참고 자료
- 공식 디자인은 [DESIGN.md](DESIGN.md) 4번 "디자인 톤앤매너"(다크 네이비 테마)이며, 화면 작업은 기본적으로 이 기준을 따른다.
- [DESIGN-STYLE-MINIMALIST.md](DESIGN-STYLE-MINIMALIST.md)는 taste-skill(minimalist-ui)에서 가져온 대안 스타일 가이드(밝은 미니멀 톤)다. 화면·컴포넌트 디자인 작업을 시작할 때 이 파일도 함께 확인해서 참고하되, 사용자가 명시적으로 minimalist 스타일 적용을 요청하기 전까지는 DESIGN.md의 다크 테마 결정을 그대로 유지한다(임의로 밝은 테마로 바꾸지 않는다).

## 작업 방식
- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 my-app 폴더 안에만 만든다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- 파일을 지워야 할 때는 바로 삭제하지 않고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.
